# OpenAIProject Context Pack

## Project Snapshot

- Product: CortexAI B2B LLM gateway (API + React frontend + CLI tooling).
- Backend stack: FastAPI + SQLAlchemy/repository pattern + PostgreSQL.
- Orchestration: provider abstraction with smart routing/fallback across OpenAI, Gemini, DeepSeek, Grok, and Claude.
- Frontend stack: React/Vite app in `frontend-react/`.
- E2E stack: Playwright tests in `e2e/`.

## Runtime Commands

```bash
python run_app.py
python run_server.py --reload
npm ci --prefix frontend-react
npm run --prefix frontend-react build
```

## Core Invariants

- Cortex grant access is operator-only through `scripts/manage_subscription_grant.py` and `server/billing/grant_service.py`; no grant HTTP route. Preserve guarded local override > valid enabled Stripe paid state > valid Cortex grant > Free. `BILLING_ENABLED` controls Stripe availability only. Grants use monthly anchored periods with `subscription_grant_id`, immutable usage history, account locks and one open grant per account. Deploy `20260905_add_subscription_grants.sql` with drained API/workers; the changed partial usage-period index requires the matching application build. See `docs/runbooks/subscription-grants.md`.

- `DATABASE_URL` is required for startup and should point to PostgreSQL.
- Most `/v1/*` endpoints accept API key, Cognito bearer, or session cookie auth.
- Session-scoped routes (`/v1/chat*`, `/v1/compare*`, including Cortex Analysis, `/v1/files/*`, `/v1/providers`, `/v1/models`, `/v1/optimize`, `/v1/history*`) reject API-key-only auth.
- Local dev can mint session cookies via `POST /v1/auth/dev-login` only when `ENABLE_DEV_SESSION_LOGIN=true` (blocked in production-like envs).
- Reverse-proxy handling defaults to `ENABLE_PROXY_HEADERS=true` with `TRUSTED_PROXY_IPS=*`. Narrow the trusted proxy list when the API is directly reachable, or disable proxy-header handling when no trusted upstream proxy is present.
- Production Cognito deployments should set `COGNITO_REDIRECT_URI` to the exact registered HTTPS callback. Automatic callback and frontend API-base derivation depend on the effective request scheme supplied through trusted proxy headers.
- Session cookies default to a seven-day lifetime (`SESSION_MAX_AGE_SECONDS=604800`); `0` restores browser-session behavior. `SESSION_COOKIE_SECURE` is an explicit override, otherwise the `Secure` attribute follows the effective HTTPS request scheme.
- Cognito JWKS and token requests verify TLS certificates by default (`COGNITO_SSL_VERIFY=true`). Disabling verification is limited to controlled local troubleshooting or trusted TLS-inspection environments.
- Frontend startup waits for Cognito/local dev-session bootstrap before session-scoped startup fetches. Signed-out Cognito users see a `Sign in to use CortexAI` workspace gate and React does not call `/v1/providers`, `/v1/models`, or `/v1/history` until a session exists; authenticated and local dev-session users hydrate Ask/Compare model selectors and the history sidebar, then restore the browser-persisted active `session_id` transcript when the page reloads/remounts.
- React account actions live in the top-right account icon menu as a secondary sign-in path. Keep the persisted light/dark theme switch available there, and keep `Log off` available as a session-clear fallback even when the icon is labelled `Guest account`; it clears the API session cookie, resets active React chat/history state, and redirects to the Cognito Hosted UI logout URL when `/v1/auth/cognito-config` provides one.
- In monolith mode (`SERVE_FRONTEND=true`), `GET /runtime-config.js` is generated at runtime and sent with no-cache headers.
- Browser lifecycle diagnostics post non-sensitive metadata to unauthenticated `POST /v1/client-diagnostics`; backend logs them as `frontend.diagnostic` for reload/discard/bfcache/long-task/error triage.
- `run_app.py` is the local full-app runner: it starts FastAPI with `SERVE_FRONTEND=false`, launches the React/Vite dev server, and passes `CORTEX_API_PROXY_TARGET` / `FRONTEND_RUNTIME_API_BASE` so Vite and runtime config point to the selected API host/port. Its loopback-only `--subscription-plan free|plus|pro|unrestricted` option provides IntelliJ-friendly local entitlement profiles; `unrestricted` removes subscription allowances only and preserves authentication, provider, file-safety, and production guards.
- The full-app runner preflights both ports before starting children and must terminate full process trees on Windows so failed startup cannot orphan Vite.
- `FRONTEND_DIR` explicitly selects the compiled static frontend directory. When unset, FastAPI uses `frontend-react/dist`.
- React dependencies belong in `frontend-react/package.json` and `frontend-react/package-lock.json`, never in Python `requirements.txt`.
- Ask and Compare support shared session continuity via `session_id`.
- History persistence remains row-oriented: React groups sidebar threads by `session_id` and reconstructs multi-model Compare turns by `request_group_id` when restoring a session. Persist each completed response-card AI-credit snapshot with routing telemetry, return it through `/v1/history`, and count the shared research component once when rebuilding Compare totals; legacy rows derive model credits from stored input/output tokens. Compare regeneration appends an immutable response revision under the original logical response root; history presents only its latest revision. Thread labels use an explicit renamed `session_title` when available, but must ignore the system-generated `API Chat` / `API Compare` placeholders and fall back to the first persisted prompt.
- React desktop and mobile history rows show each thread's latest activity timestamp using Today/Yesterday/calendar-date formatting.
- Desktop history rows expose a hover/focus context menu containing only Rename and Delete; Rename persists `sessions.title` through `PATCH /v1/history/session/{session_id}`, while Delete uses the short in-row confirmation and removes each persisted request row in the selected thread. Mobile keeps its existing inline delete affordance. `DELETE /v1/history?session_id=<id>` clears only that session, while omitting `session_id` clears all history for the authenticated identity.
- Expanded desktop sidebar history is the `Recent` list: show up to the 100 newest grouped chat threads in Today/Yesterday/month-day groups with single-line 36px rows containing a compact 11.5px ellipsized title and narrowed `MODE · time` caption that swaps to the context-menu trigger on hover/focus. Do not add a leading mode glyph; preserve at least 140px for title text at the 280px expanded width. Keep model names, turn counts, and token counts hidden, preserve the full title in the row tooltip, and support arrow navigation plus Enter/R/D/Escape. Leave the collapsed 76px desktop rail and separate mobile History layout unchanged. Ask, Compare, Usage, AI credits, and Models live in the desktop rail; the bottom-left session tile is status-only for active sessions and must not trigger logout; the mobile bottom navigation remains Ask/Compare/History only while Usage, AI credits, and Models are reached from the account menu.
- Explicit browser fresh sign-in starts a new chat session instead of restoring the previously active thread; page refresh, Chrome tab resume/reload, same-browser reauth, and explicit History thread selection still preserve continuity for the selected session. React uses `cortex_active_session_id` for active-thread persistence and consumes `cortex_fresh_login_pending` plus `fresh_login=1` only for intentional fresh-login resets.
- Conversation-history guardrails are active, but oversized history is soft-trimmed instead of rejected.
- Compare targets are explicit; smart routing flags are ignored in compare mode by design.
- `config/model_registry.yaml` is the only production model-catalogue and provider-pricing source. It owns selectable and compatibility identities, lifecycle/replacement/alias records, effective-dated standard/long-context/cache prices, official pricing/lifecycle URLs, and `source_verified_at`; `config/pricing.py`, smart routing, `/v1/models`, and the React Models screen consume it. Every provider price/lifecycle refresh must be checked against the official provider pages. Preserve requested, provider-served, and pricing model identities plus the pricing rule/version/snapshot in responses and history. Provider reasoning usage is auditable but is not double-billed when included in output. Unknown exact pricing must use a marked conservative provider fallback and must never silently become zero.
- Active selectable Claude 4 options deliberately include Sonnet 4.6 as `advanced` for Plus/Pro and Opus 4.5/4.6 as `premium` for Pro. Their plan placement must continue to flow from the canonical registry billing class and subscription-plan class allowlists, not a separate model-name allowlist.
- Consumer plans are server-owned in `config/subscription_plans.yaml` and must validate at API startup. The only monthly usage meter is `ai_credits`: Free/Plus/Pro receive 100,000/1,000,000/3,000,000 credits. Model access categories (`economical`, `standard`, `advanced`, `premium`) are distinct from smart-routing tiers (`T0`-`T3`), and every registry model must explicitly declare its category, input/output credit multipliers, usage label, and pricing version. `server/billing/subscription_service.py` is the only effective-plan lifecycle authority and falls back conservatively to Free; `server/billing/entitlement_service.py` owns feature/model/file decisions; `server/billing/metering_service.py` owns atomic reserve/supplement/settle/release/expiry transitions; `server/billing/enforcement_service.py` composes those services for DB-mode Ask/Compare/Optimize/Cortex Analysis. Routes use the same clamped output ceiling for provider work and billing, settle actual successful input/output credits, reserve 10,000 credits for normal Advanced Web Search, and settle fresh Tavily calls as `provider credits used x 5,000`; omitted provider usage uses a marked-estimated two-credit fallback, while cached/reused research is free. Routes release unused estimates/failures and partially settle Compare. An underestimate supplements atomically when capacity exists; otherwise billing is capped at the authorized amount, the wallet never goes negative, and an adjustment ledger item records the unbilled difference. Improve Prompt reserves every possible attempt. Smart routing ranks plan-allowed candidates, filters them by affordability, reserves the first appropriate affordable candidate, and requires supplemental authorization before a more expensive fallback; do not add an arbitrary percentage buffer. Upload/storage is free. `credit_transactions` is the immutable itemized reconciliation ledger exposed by `GET /v1/credits/transactions`; every newly settled model, research, and adjustment item carries the initial query after storage-policy sanitization plus an optional display-only credit activity ID, while metadata-only mode omits the query and legacy rows return a null query/request-ID grouping fallback. React must share one activity ID and original prompt across Prompt Optimizer and its following Ask/Compare request; neither field may affect billing authority or arithmetic. PostgreSQL startup fails before provider traffic when any required billing schema object is missing, and the API automatically heartbeats active reservations and cleans stale ones. Verified Stripe webhooks remain authoritative for Stripe state; valid Cortex grants provide paid-plan fallback even with hosted billing disabled.
- React presents one customer-facing AI credit per 1,000 raw backend credit units through `frontend-react/src/utils/aiCredits.ts`. Apply that formatter to every visible credit amount and matching accessibility value, including plans, allowances, response/Compare statistics, history, structured errors, and Work. Never scale API contracts, database values, ledger/reservation arithmetic, in-memory credit state, or outgoing request payloads.
- Ask/Compare generation policy is server-owned in `config/generation_profiles.yaml` plus model-native capability fields in `config/model_registry.yaml`. `orchestrator/generation_policy.py` is the only resolver: v3 Auto selects 4K for normal economical/standard tasks, 8K for advanced reasoning models, and 12K for premium or deterministically complex/detailed tasks; explicit Quick/1K, Balanced/4K, Deep/12K, and Extended/32K remain available to API callers and retries. It clamps budgets to model/context/operational bounds, rejects unsafe explicit custom limits, maps provider reasoning controls, and returns the exact per-target ceiling used by both provider execution and billing authorization. Claude capabilities must fail closed: only registry-declared adaptive models receive adaptive thinking/effort, manual-budget-only Claude 4.5 models default to normal generation until the public contract supports `budget_tokens`, and `api/claude_client.py` omits custom temperature for adaptive-thinking and Claude 5 requests. Credit-aware authorization may reduce the ceiling before provider execution while the shared Compare reservation remains authoritative. React sends Auto without exposing an Answer depth selector or live hold estimate; requested detail belongs in the prompt. Reasoning tokens may consume provider output allowance and remain billable work; length-limited results preserve partial text as `incomplete/token_limit` and offer a new-call retry at Deep or Extended. Persist request budget/reasoning decisions and response completion status, expose them through history, and keep `POST /v1/billing/estimate-generation` as an API-only no-reservation maximum-hold preview. `GENERATION_BUDGET_POLICY_ENABLED=false` is the reviewed rollback switch to historical Quick/2K execution.
- Ask/Compare settlement and response DTO statistics must use the canonical requested model for consumer-credit lookup while preserving served/pricing identities for provider-cost audit. Per-response credits, aggregate Compare credits, and ledger charges must stay reconciled when providers return versioned served-model suffixes. Such suffixes must not block aggregate Compare settlement, and any finalization failure must release and unregister its reservation so the heartbeat worker cannot indefinitely protect failed work from stale cleanup.
- Cache-aware AI-credit accounting extends the existing credit economy rather than replacing it. `server/billing/credit_calculator.py` is the single formula authority for ledger settlement and user-visible credits: normal/cache-read/cache-write components round up independently, effective provider-price ratios scale the existing input multiplier, invalid provider partitions are clamped, and missing pricing never discounts. Reservations assume no hit and cover expensive writes; calculation defaults on in shadow mode while cache-aware settlement defaults off. Provider cache behavior, persistent research/optimizer/Cortex reuse, credit-aware output ceilings, and context compaction remain separately reversible. Cache affinity uses HMAC-SHA256 and never embeds user/session/prompt/file content. Compare performs one research call and one request-level reservation. Per-request `cache_reuse_events` rows are the reporting authority for research, optimizer, and Cortex reuse rates. Large-file extraction remains SHA-deduplicated and narrow questions reuse query-relevant persisted chunks.
- Cortex Work settlement treats Managed Agent normal input, cache reads, cache writes, output, active runtime, and web searches as independent cumulative deltas. `server/work/billing.py` reconstructs the component charge and applies the provider's cumulative USD `list_cost` delta as a minimum AI-credit floor; malformed or non-USD provider cost snapshots fail reconciliation and retain the reservation for investigation rather than underbilling. Billing identity comes only from the retrieved Managed Agent session's resolved Agent snapshot and is persisted per run; missing, unknown, or multi-model identity fails closed without a billing-model environment fallback.
- Stripe hosted billing sessions keep credentials, paid-plan Price IDs, redirects, provider payloads, signature verification, and response validation in `server/billing/stripe_gateway.py`. `server/billing/session_service.py` separates hosted-session provider calls from short DB transactions, atomically claims Customer identity, and sends provider-live accounts to Portal instead of duplicate Checkout. `server/billing/webhook_service.py` persists verified provider events before row-locked processing, handles required Checkout/subscription/invoice lifecycle events, reverse-maps server-owned Prices, rejects stale snapshots, preserves same-period usage counters, applies grace/cancellation through `subscription_service.py`, and provides internal reconciliation. Strict request schemas reject client Price/amount/currency/Customer/redirect fields. `BILLING_ENABLED=false` returns `billing_not_configured`; hosted-session creation never grants paid access, and verified Stripe webhook state is authoritative. Do not expose reconciliation over HTTP until administrator authorization exists.
- React subscription state is in-memory and auth-aware: `frontend-react/src/api/billing.ts` and `api/entitlements.ts` own transport, `subscription/subscriptionErrors.ts` owns typed safe errors, and `hooks/useSubscription.ts` loads authenticated state only after auth bootstrap. Checkout-return query state is a polling hint, never paid authority; only webhook-synchronized `/v1/entitlements` may confirm a paid plan. Signed-out users may fetch the public display-safe plan catalogue but must not call current-subscription or entitlement endpoints. React follows only validated backend-returned HTTPS hosted URLs and never calls Stripe directly or stores billing authority in browser storage.
- React plan management lives at `/pricing` and `/account/billing`, using the shared responsive subscription shell and summary-only account-menu plan context. `GET /v1/billing/plans.billing_enabled` is the only public hosted-billing availability signal; it is boolean and display-safe. Pricing and Billing must render lifecycle and allowance state from `useSubscription`, keep paid plan switching in Portal, and never promote a Checkout return to paid access before webhook-synchronized entitlements confirm it.
- React subscription locks are explanatory UX, never billing authority. Model access joins static display IDs to live `/v1/models.billing_class`; missing metadata fails conservatively. Compare target count, Web, Improve, attachment count/size, uploaded bytes, and monthly counters come from live plan/entitlement contracts. Structured subscription denials open the shared accessible dialog, preserve the current composer draft and attachments until a stream is accepted, and never hide restored premium history after downgrade.
- Cortex Analysis is user-triggered only after two or three successful Compare responses. It defaults to the all-plan standard model `gpt-5.4-mini`, anonymizes and shuffles response identity before generation, restores every user-visible `Response A/B/C` reference to its canonical provider-and-model display name after generation, validates strict structured output, and appends each successful run to `cortex_analysis_runs`. Disagreements are persisted and returned as attributed `{who,text}` positions with nullable `disagreementNote`; legacy flat-string rows remain readable through DTO compatibility. Same-provider comparisons must remain model-distinct in every analysis section. The finished React result is an always-visible continuous document with no disclosure state; conflict leads visually only when disagreements exist and confidence is limited. Creation/regeneration is a new synthesized model call charged to the unified AI-credit wallet for its Compare question/source input and actual output; it has no separate quota and does not charge Tavily again when reusing Compare research. Every run must survive reload and History reopening and remain readable after downgrade; regenerating a source response keeps older analyses readable but stale. During an analysis re-run, show only the processing state and temporarily hide the saved result; restore the saved result if the re-run fails. Both analysis endpoints must preflight the migration schema, including `disagreement_note`, and return `503 cortex_analysis_schema_unavailable` before provider work when it is absent or incomplete. Do not add automatic synthesis, trial behavior, or a Cortex-only mobile tab without an explicit product decision.
- Compare web research is prepared once per compare turn and shared across selected targets; underspecified follow-up search queries should anchor to the previous user topic before retrieval.
- Ask and Compare must preserve non-empty successful provider answer text. Do not add Cortex-authored phrase, number, date, citation, or alleged-fabrication scanners; optional grounding belongs to Web research, while provider-native safety/errors, empty-output normalization, and Smart fallback remain separate concerns.
- Frontend Compare selectors keep at least two active models. Empty initial slots wait for effective entitlements and choose defaults only from models whose billing class is allowed by the current plan, while the picker retains the complete catalogue and valid existing/manual selections. Within the eligible set, prefer `openai:gpt-5.6-luna` and `claude:claude-sonnet-5`, prefer `deepseek:deepseek-v4-flash` for Add Model, and fall back to distinct selectable catalogue models when a preference is unavailable. With three active models, removing any slot compacts the remaining two instead of allowing the default resolver to refill the removed slot.
- Frontend manual Ask selection waits for both catalogue and entitlement loading. When Smart routing is disabled and no valid manual selection exists, use `openai:gpt-5.6-luna` for Free, `claude:claude-sonnet-4-6` for Plus, and `openai:gpt-5.6-terra` for Pro. Keep all catalogue models visible with normal plan locks and preserve valid existing/manual selections across Smart on/off toggles.
- React manual Ask and Compare controls share `ModelPicker`, an accessible provider-first listbox. Opening shows provider logos and model counts. Fine-pointer desktop layouts reveal an adjacent provider-model panel immediately on hover, keep it stable while crossing between panels, swap it on provider hover, and dismiss it shortly after leaving the picker; click and Right/Left Arrow remain fallbacks. Touch/mobile layouts tap into a compact single-column model list with an in-menu Back action. Model rows retain readable family labels, exact IDs, credit-use hints, plan locks, duplicate state, and selected-state styling. Compare supplies duplicate-option disabling and removal behavior. Keep the synchronized hidden native selects because the E2E harness uses `#singleModel` and `#compareModel1..3` for option enumeration, `inputValue`, and `selectOption`.
- The styled model picker resets to providers whenever it reopens. Its fixed-position menu renders through a body portal so narrow Compare overflow containers cannot clip either the desktop cascade or touch drill-down; synchronized hidden native selects remain available only for automation compatibility.
- React frontend assistant responses use `frontend-react/src/components/results/` with progressive Ask/Compare stream rendering, visible copy-success confirmation, inline publisher-name citation pills from `web_source_items`, code-block copy controls, GFM table rendering, sanitized provider errors, and no active auto-follow or floating down-arrow jump control during streaming. Response tables use contained horizontal scrolling on desktop and header-labelled stacked rows on mobile.
- Chat and Compare streaming may emit `heartbeat` NDJSON events while provider work is pending. React ignores these visually; they exist to keep intermediaries from treating long provider calls as idle response bodies and are logged through stream lifecycle telemetry.
- React response headers resolve labels and provider logos through `config/modelPresentation.ts` and the shared `components/shared/ProviderLogo` renderer used by `ModelPicker`; do not add response-only provider mappings.
- The React Usage & insights route uses backend-driven provider analytics on desktop and mobile. Phone layouts shorten the visible title and KPI labels, keep the accessible heading as `Usage & insights`, show the Smart-pick leaderboard strip and session-mode bar, and retain the Ask/Compare/History footer navigation with mobile account-menu entry. Unified balance/reset and credit history live separately at `/credits` under the explicit `AI credits` navigation label. The screen must group itemized transaction rows by display-only `activity_id` into one activity card with the original pre-optimization query and total first; Prompt Optimizer and its following Ask/Compare request share that ID. When both optimizer and final model rows exist, combine them into one `Final optimized ... answer` breakdown line that names the included optimizer attempts and answer generation; retain a distinct label only for optimizer-only activity. Keep Compare, Cortex Analysis, and Web Search details collapsed until requested, aggregate optimizer retries, omit zero-credit adjustments from the visible breakdown, and retain the request-ID/unavailable-query fallback for legacy or privacy-policy-limited activity.
- Empty streaming responses use a per-card `ResponseLoadingState`. Each card removes its own loading block on first text/error, so faster Compare targets do not wait for slower targets; shimmer and pulse animations must remain disabled under `prefers-reduced-motion`.
- Smart Ask pending response headers remain model-neutral because the stream `start` provider/model is only a pre-runtime preview. Show `Smart routing` while pending and use the authoritative provider/model from `response_done`.
- Appending a new Ask or Compare turn always performs one smooth reveal of the submitted question, including when the user was viewing an older turn. Response-card regeneration refills the clicked response card in place, preserving the original turn's source-enabled flag so source-backed regenerations issue a fresh backend research/search request. Regeneration targets only the clicked response model, including for cards that came from a Compare turn, and streams through the chat endpoint without adding a new UI card. Subsequent response growth does not continue moving the viewport.
- React response sources render inline as citation pills. On desktop, hovering a pill opens a popover listing each grouped source link and leaving the hover area closes it. On phone-sized mobile, tapping a pill still opens the bottom sheet. The pill's external-link icon opens the first cited source directly.
- React Compare keeps every selected response visible in a responsive grid without horizontal response scrolling on desktop and tablet widths: three columns on wide desktop, two at tablet widths, and stacked tall cards at the app's tablet/mobile shell breakpoint. Phone-sized mobile uses a segmented model switcher, shows one selected response card at a time in natural page flow, and elevates the stuck switcher into a frosted provider-tinted bar.
- Compare transcripts reserve bottom breathing room above the persistent composer so the input area does not compress the reading workspace. Desktop/tablet Compare cards keep fixed headers/footers and independently scrolling response bodies; phone-sized mobile uses normal page scrolling for the active response card. Ask turns remain content-sized.
- React Ask and Compare share the same right-aligned user-message bubble, including attachment and optimization states. Optimization status renders as a right-aligned pill below the bubble so the prompt body keeps normal case and full wrapping. Compare keeps aggregate totals in a separate compact row, shows friendly model labels beside exact API model IDs, and uses compact icon actions.
- Mobile and desktop response cards show completed per-model duration and AI-credit usage directly in the header without a run-details chevron; the fastest label is desktop-only while its success tint remains visible on mobile. Compare summaries show success/error counts and aggregate AI-credit usage without token totals. Pending and failed response cards keep their muted elapsed/status line visible on mobile and desktop. Response-card duration uses the same UI-observed elapsed timing when live timestamps are available and falls back to API `latency_ms` for restored rows. Keep token usage in the API contract and React state for persistence and reporting, but do not render it on Ask or Compare results.
- Latest completed Ask response cards may show deterministic suggested follow-up chips extracted from clear assistant-offered options, one concrete follow-up offer, or a quoted follow-up query. The row is owned by `ResponseCard`, appears below markdown and above response actions, submits chip text through the normal chat stream path, skips Improve optimization, and is not shared with Compare cards or composer feature chips.
- React Ask and Compare share one rounded composer shell: model options sit above a borderless textarea that starts at one line and auto-grows to a bounded height, attachment chips grow upward inside the shell, and compact feature controls plus the fixed-size send action share the bottom row. Compare model chips are separated by decorative opposing-arrows connectors; desktop uses a subtle bordered medallion while mobile uses a narrow borderless glyph. Mode selection stays in the main navigation, while mobile compare chips scroll horizontally when needed and the answer-screen follow-up composer rests as a docked context pill above the fixed bottom navigation until tapped open. Mobile answer transcripts reserve bottom scroll clearance so response copy, regenerate, and feedback actions stay reachable above the dock. The shared model picker renders its fixed-position listbox through a body portal so horizontally scrollable mobile Compare rows cannot clip it.
- Keep the React composer shell visually borderless: use a transparent structural border to avoid layout shift, soft idle/focus elevation, and no textarea browser outline on desktop or mobile.
- Mobile starts new sessions through one persistent square-pen action in the header. It cancels active generation, clears the current thread, returns from History to chat, and preserves Ask/Compare mode; do not duplicate New chat inside mobile History.
- Shared React feature chips keep Smart, Web/With sources, and Improve guidance in accessible `role="tooltip"` elements. Ask starts with Web enabled and Compare starts with With sources enabled for new page sessions, while preserving manual off choices. Reveal tooltips on hover and keyboard focus; on touch input, one tap toggles the chip and shows its tooltip for two seconds. Keep tooltip bounds inside narrow mobile viewports.
- React attachment selection follows upload capability booleans exposed by `/runtime-config.js`. When `ATTACHMENTS_DIRECT_UPLOAD_ENABLED=true`, it validates the complete batch from entitlements before authorization, keeps raw `File` objects in a transient task store, requests metadata-only `/v1/files/upload-intents`, uploads directly to the returned S3 POST with real `XMLHttpRequest` progress and concurrency two, calls `/complete` only after S3 success, and promotes only Cortex-verified `ready` files into `chatStore.attachments`. Pending or failed selected tasks block Ask/Compare Send; retry, abort/remove, bounded polling, and partial multi-file success remain per-file. When direct mode is off, the same queue UI uses multipart `/v1/files/upload-batch` while `ATTACHMENTS_LEGACY_PROXY_UPLOAD_ENABLED=true`; raw-byte `/v1/files/upload` remains a one-file compatibility contract. `DELETE /v1/files/{file_id}` transitions to `deleting` and reuses cleanup. S3 stays private; optional `ATTACHMENTS_S3_SERVER_SIDE_ENCRYPTION`/`ATTACHMENTS_S3_SSE_KMS_KEY_ID` values become exact signed form fields and legacy PutObject arguments. The repository has no AWS IaC, so production CORS, IAM/KMS/bucket policy, WAF, signing identity, and cleanup topology remain operator-verified rollout gates.
- CortexAI Work is a separate `/work` surface and Zustand store backed by durable PostgreSQL sessions/runs/events, Anthropic Managed Agents, the existing private-S3 file pipeline, subscription limits, and unified AI-credit reservations. Keep `CORTEX_WORK_ENABLED=false` until both `db/migrations/20260820_add_cortex_work_mode.sql` and `db/migrations/20260829_add_work_web_output_and_model_identity.sql` plus `docs/runbooks/cortex-work.md` production gates pass. Routes and services stay provider-neutral; all provider/MCP/S3/OAuth calls happen outside DB transactions. Reconciliation uses PostgreSQL leases and idempotent provider events, runs in an always-on application worker as well as request paths, and keeps SSE resumable by sequence/`Last-Event-ID`. Each run snapshots a server-resolved `auto|on|off` Web decision, requests finalization at 32K output tokens, interrupts at the server-owned 40K ceiling, and preserves the distinct `output_limit_reached` partial-result status. React must retain a newly created Work session before starting its first run so structured denials retry in place, sidebar Work history must omit zero-run shells, and an opened session must hydrate every chronological run so earlier prompts, visible outcomes, and artifacts remain in the transcript after a follow-up. While run acceptance is pending, React must replace the landing composer with a `Starting work` workspace, preserve the submitted instruction, and avoid fabricating a run ID or Stop control. Work titles are normalized to provider-safe single-line values both before persistence and immediately before Managed Agent creation so existing unsafe titles recover on retry. When reconciliation reports a terminal run ahead of the current stream cursor, React must merge the remaining durable events before stopping so the final agent message never requires a refresh. The Work rail hides unlabeled provider progress telemetry, animates only the latest visible event while a run is nonterminal, and settles all activity markers and plan steps after completion. Provider-native file/web READ tools run automatically; MCP READ is auto-confirmed by Cortex, exact remembered WRITE grants are session+connection+tool scoped, and bash/write/edit plus destructive/external/financial/deployment actions require explicit approval. Provider session budgets enforce the credit stop without polling interrupts; reused sessions extend the cumulative cap, baseline earlier events, and update Web/MCP tools in place before a follow-up. Only incompatible vault-resource changes replace the provider session, with bounded replay of prior user instructions and visible outcomes. Artifact imports use the originating run's provider session, skip input/non-downloadable files, isolate failures per file, and retry idempotently when a terminal run's artifact list is requested. Never expose provider thinking, provider file URLs, raw tool payloads, tokens, or credentials.
- Tavily research uses a deterministic local search-options resolver before provider calls. The resolver fixes retrieval params, may add topic/time/country/domain options when enabled, never rewrites the query, and can be reduced to fixed params with `TAVILY_ENHANCED_SEARCH_ENABLED=false`.
- Frontend follows the Alabaster Minimal shell: a quiet 272px desktop navigation rail that can collapse to a narrow icon rail, subtle mode and current-session states, top Ask/Compare tabs, prompt starter landing, horizontal compare canvas, unified composer with active model chips, visually aligned mobile Ask/Compare/History navigation, and token-backed light/dark themes switched from the account menu.
- The React Ask empty state uses product-focused workspace copy and four responsive task examples for debugging, document summaries, writing refinement, and file analysis. Selecting an example only fills the prompt; it must not auto-submit.
- The React Compare empty state uses the same hero system with Compare-specific workflow copy and three responsive examples for system design, debugging, and multi-model review. Selecting an example only fills the prompt and must preserve the current Compare mode and model selections.
- Frontend-only development falls back to a bundled display model catalog when `/v1/models` is unavailable; bootstrap history failures should not set the primary chat error banner.
- The React Models route (`/models`) builds production rows from live `/v1/models`; `frontend-react/src/config/models.data.json` contains presentation structure only, and `defaultModels.ts` is the small offline fallback. The screen never grants model access and must not duplicate the server catalogue or provider prices.
- Prompt optimization for the UI goes through explicit `POST /v1/optimize`; chat/compare auto-optimization is off by default and requires `ENABLE_ORCHESTRATOR_PROMPT_OPTIMIZATION=true`.
- Explicit UI prompt optimization is latency-bounded (`PROMPT_OPTIMIZER_TIMEOUT_MS`, default 5000 ms), defaults to two route-level attempts, retries unchanged output once for locally classified weak prompts, rejects answer-like or unresolved-placeholder optimizer output, and sends compact mixed user/assistant context whenever the active thread has prior messages. Attachment file contents are not copied into optimize requests. React caps optimize context to the last ten mixed messages and a 4,000-character `context_hint`; the backend prefers structured `context.conversation_history` over the flat hint when building the optimizer prompt. The Improve pending state uses one stable live status pill below the user bubble, respects reduced motion, and reveals optimized or kept-original status without replacing the prompt body with status text.

## High-Value Paths

- API routes: `server/routes/`
- API schemas: `server/schemas/`
- Effective subscription endpoint: `server/routes/entitlements.py`; display-safe plan/current-subscription routes: `server/routes/billing.py`; lifecycle/decision services: `server/billing/`
- Orchestration: `orchestrator/`
- Provider clients: `api/`
- Persistence and reporting: `server/persistence.py`, `server/usage_reporting.py`, `server/savings.py`, `db/`
- Config single sources:
  - `config/providers.yaml`
  - `config/model_registry.yaml`
  - `config/subscription_plans.yaml`
  - `config/pricing.py`

## Change-Impact Map

- New endpoint: route + schema + router wiring + tests + README API section.
- Provider/catalog changes: `api/`, `api/client_registry.py`, `config/providers.yaml`, provider discovery tests.
- Subscription-plan, lifecycle, entitlement, or billing-class changes: `config/subscription_plans.yaml`, `server/billing/`, `server/routes/entitlements.py`, `server/routes/whoami.py`, `config/model_registry.yaml`, `orchestrator/model_registry.py`, matching schemas/docs/Postman, and billing/model contract tests.
- Routing behavior changes: `orchestrator/prompt_analyzer.py`, `tier_decider.py`, `model_selector.py`, `smart_router.py`.
- DB changes: add an idempotent forward SQL migration under `db/migrations/`, reflect it in table/repository usage, add it to every official apply sequence, and keep the PostgreSQL startup schema preflight aligned.
- Billing repository and metering-service functions do not commit. Their callers own transaction boundaries. Reservation creation locks the billing account to serialize account-scoped idempotency keys, then locks counters in deterministic meter-key order; settlement, release, and stale expiry lock the reservation before its counters. Never hold these transactions open across provider calls.
- Ask/Compare billing reservation IDs include a server-generated suffix even when the client supplies `X-Request-ID`, preventing a replayed correlation ID from reusing a terminal reservation for a new provider call.
- `/v1/entitlements` and DB-mode `/v1/whoami` use committing units of work because effective resolution may lazily create a billing account and current usage period. `plan_tier` is compatibility-only; new consumers use the entitlement snapshot and `billing.plan_code`.
- Frontend behavior changes: `frontend-react/` plus React tests/build and `e2e/` contract checks as needed.
- React subscription-data changes: keep server contract calls in `frontend-react/src/api/billing.ts` and `api/entitlements.ts`, auth-aware state/polling in `hooks/useSubscription.ts`, typed errors in `subscription/subscriptionErrors.ts`, and matching tests/docs. Never introduce a browser-side plan authority.

## Validation Matrix

- Unit/regression baseline:
  - `python -m pytest -q`
- Release gate:
  - `python scripts/release_gate.py`
- Pricing/registry consistency:
  - `python -m pytest tests/test_registry_pricing_alignment.py -q`
- CI changed-file quality gates:
  - Ruff/MyPy run on added or modified Python files; deleted paths are excluded before tooling runs. Black is advisory until a formatting baseline lands.
  - Gitleaks scans the checked-out tree with the pinned CLI rather than repository history.
  - Install the mandatory [local Git gates](./ci-commit-gate.md) once per clone. `pre-commit` blocks staged Gitleaks/Ruff/MyPy failures; `pre-push` runs applicable locally available `ci.yml` jobs against the committed branch and blocks failures. Both hook stages and `scripts/release_gate.py` isolate pytest in a repository-private temp root so Windows user-temp ACL failures do not mask test results. Missing Docker defers only the API image build to GitHub Actions unless `CORTEX_CI_REQUIRE_DOCKER=1`.
- Frontend local checks (when UI touched):
  - `npm run --prefix frontend-react build` when React UI is touched
  - `npm run --prefix frontend-react test` when React component logic/tests are touched
- Full browser E2E:
  - `npm run --prefix e2e test`
- Frontend-only responsive browser suites:
  - `npm run --prefix e2e test:mobile`
  - `npm run --prefix e2e test:desktop-ipad`
  - These suites start isolated Vite servers and mock frontend API contracts; they do not require PostgreSQL or live provider credentials.

## Delivery Defaults

- Keep handlers thin and move logic into service modules.
- Preserve backward compatibility for public API contracts unless migration is explicitly requested.
- Update docs for behavior changes:
  - `README.md`
  - `docs/postman/CortexAI_B2B.postman_collection.json`
  - runbooks/ADRs when architecture decisions change.
